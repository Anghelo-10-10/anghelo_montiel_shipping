spring:
  application:
    name: shipping
  datasource:
    url: jdbc:postgresql://localhost:5438/shipping_db
    driver-class-name: org.postgresql.Driver
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 9000